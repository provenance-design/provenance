# Build / dependency artefacts
node_modules/
mcp/node_modules/
.next/

# Mac noise
.DS_Store
**/.DS_Store

# Private working materials — never push to public repo
# NOTE: git does NOT support inline comments on pattern lines — keep comments on their own line.

# Springer paper drafts under peer review
docs/paper/
# Multi-AI collaboration memos (private workflow)
docs/collab/
# Internal evaluation results
docs/quality-tests/
# Planning docs, specs, plans (internal)
docs/superpowers/
# Internal reference material
docs/reference/
# Internal design/spec docs
docs/FAE-REFERENCE.md
docs/PROVENANCE-VISUALISER-DESIGN-GUIDANCE.md
docs/VISUALISER-DESIGN-SPEC.md
# Agents' working notes
.claude/agent-memory/
# Claude Code session memory (path-encoded; identity-leaking)
.claude/projects/
# Session state
.superpowers/
# Internal paper-review agent personas (reference the unpublished paper / journal)
.claude/agents/red-journal-reviewer.md
.claude/agents/academic-voice-reviewer.md
.claude/agents/design-epistemology-reviewer.md
# Collab inbox tooling
tools/check-inbox.sh
# Local knowledge-base config (PalaceForge; lists identity)
entities.json
palaceforge.yaml

# Third-party content (not ours to redistribute)
docs/*.pdf

# Local environment
.env
.env.local
.env.*.local

# Backups
*-BACKUPS-*/
*.tar.gz
*.bundle
